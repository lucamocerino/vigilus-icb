# Render Blueprint — deploy automatico da GitHub
# Backend su Render + DB su Supabase (free illimitato)
# https://render.com/docs/blueprint-spec

services:
  # ── Backend API ──
  - type: web
    name: vigilus-api
    runtime: python
    region: frankfurt
    plan: free
    rootDir: backend
    buildCommand: |
      pip install . &&
      python -m spacy download it_core_news_sm &&
      python -c "from sentinella.nlp.classifier import get_classifier; get_classifier()._download_model(); print('ONNX model downloaded')" &&
      python -c "import sentinella; print('Build OK')"
    startCommand: uvicorn sentinella.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false  # Impostare manualmente con URL Supabase
      - key: DEBUG
        value: "false"
      - key: API_KEY
        value: ""
      - key: RATE_LIMIT_PER_MINUTE
        value: "120"
      - key: CORS_ORIGINS
        value: "https://vigilus-frontend.onrender.com"
      - key: SCHEDULER_ENABLED
        value: "true"
      - key: SCHEDULER_INTERVAL_MINUTES
        value: "30"
      - key: PYTHON_VERSION
        value: "3.12.0"

  # ── Frontend (static site) ──
  - type: web
    name: vigilus-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: "https://vigilus-api.onrender.com"
    headers:
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
